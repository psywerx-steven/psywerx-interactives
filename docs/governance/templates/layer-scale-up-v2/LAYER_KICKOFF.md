# Layer Scale-Up V2 — Layer kickoff prompt

Audit `[LAYER]` from frozen commit `[SHA]`. This run is candidate-only and grants no governance or activation authority.

1. Inventory every Family, Driver, RDS, existing incident Relationship, prior candidate, source registry, and protected baseline.
2. Assign stable ownership and cross-Family consultation routes.
3. Produce coverage rows for every Driver without candidate quotas.
4. Mark RDS as calculated states and prohibit direct effect targeting.
5. Preserve production science byte/object semantics.
6. Route ontology, classification, architecture, alias, and target-semantics problems to explicit blockers.

Return the deterministic inventory, missing inputs, reusable prior work, protected paths, and Stage 1 Family order. Do not begin deep candidate research in this step.
