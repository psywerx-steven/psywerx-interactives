# Informational architecture and ontology escalations

**ADVISORY — HUMAN DECISION REQUIRED. No blocker is resolved here.**

| ID | Exact unresolved issue | Effect on this audit |
|---|---|---|
| `HYP-INF-F03-H20` | Frozen `INF-013` conceptual-complexity definition includes surface-language features overlapping `INF-077`; the latter retains blocked metadata. `INF-014` readability depends partly on surface inputs. | Pilot's `REL-INF-006` replacement cannot be implemented; no definition, alias, classification or feature-scope workaround is made. Existing reviewed science stays intact. |
| `ARCH-INF-LAYER-0001` | `INF-004`, `INF-010`, `INF-011` and `INF-068` are active causal sources without per-record versioned exact derivation/aggregation rules in frozen metadata. | D10 heightened causal-source review and constituent/aggregate double-count safeguards are required before any new execution; this audit makes no architecture change. |
| `META-INF-LAYER-0001` | `RDS-0001` Message Cohesion has blocked mechanism, timing, observability and evidence fields. | No silent metadata repair; no new candidate depends on it. |

The seven RDS retain their frozen definitions and declared scales. Their exact input sets, aggregation, units, windows and baselines are `NOT_EXACTLY_DECLARED` in the audit where production does not specify them. That is a recorded limit, not an inferred formula.

| RDS | D10 risk disposition |
|---|---|
| `INF-004` | Heightened causal source and target; prominence input/competition rule unversioned. |
| `INF-010` | Heightened causal source and target; requirement-set and completeness aggregation unversioned. |
| `INF-011` | Heightened causal source; contradiction/exposure rule and root antecedents need review. |
| `INF-014` | Heightened causal target; INF-013/INF-077 overlap blocked by H20. |
| `INF-053` | Noncausal composition/derivation only in current incident set; exact audience-fit rule still needed for execution. |
| `INF-068` | Heightened causal source; omission reference universe and root antecedents need review. |
| `RDS-0001` | No active causal incident edge; metadata remains blocked. |

There are nine unique causal edges incident to an Informational RDS. Each receives D10 `HEIGHTENED` review; there is no active RDS→RDS causal edge requiring `EXCEPTIONAL` review. No direct EffectAssertion target is an RDS. `ASTRA-INF-LAYER-001` queues the H20 ontology question for possible future specialist review while keeping the current decision blocked; Layer completion does not wait for that review.
