# Informational Layer V2 governance recommendations

**ADVISORY — HUMAN DECISION REQUIRED. Nothing below is governed, materialized, registered or activated by this audit.**

## Executive summary

Program `AUD-INFORMATIONAL-LAYER-AE-V1-20260920-001` is a candidate-only review of 13 Families, 71 Drivers, seven RDS and 64 incident Relationships against main `236b9c6bd0642a4704f3a845454846bb13a09def`. The 348 governance-index rows compress to **six grouped human decisions + eighteen individual scientific decisions + two blocked decisions**. The index also contains 255 non-voting row acknowledgements; 26 existing Relationship rows link to prior INF-F03/Psychological human decisions and are not voted again. Repeated Family, source, evidence and workflow references do not create new scientific claims.

The only newly recommended future governed record is one **inactive operation identity** (`HT-CAND-INF-LAYER-0001`). The new `INF-061 → PSY-016` Relationship candidate remains research-needed after skeptical review. There is no recommended new EffectAssertion. Future materialization is **0 Relationships, 1 HappeningType, 0 EffectAssertions, 0 EvidenceAssessments**, conditional on exact human approval and later source verification. Current new `GOVERNED = 0`, `ACTIVE = 0`.

## Grouped approvals recommended

- `GRP-INF-001` — `APPROVE_RETAIN` for five unchanged prior governed/noncausal edges (`REL-CUL-054`, `REL-INF-049`, `REL-INS-049`, `REL-RDS-0007`, `REL-TEC-053`). This confirms no new scientific change.
- `GRP-INF-002` — `APPROVE_RETAIN_V1_INCOMPLETE` for five bounded Technological-to-Informational edges (`REL-TEC-048/051/052/065/067`). Retention grants no additional execution scope.

The structured package lists every record; a grouped vote does not erase its individual caveats.

## Grouped rejection recommendations

`GRP-INF-004` accepts five category-error rejections: availability→attention, frame→stable loss-aversion trait, threat content→behavior, correction→universal debiasing, and guidance→action. These are durable hypothesis dispositions, not production deprecations.

## Grouped research-needed recommendations

- `GRP-INF-003` keeps twelve existing Relationship propositions research-needed because their exact mechanism, endpoint, exposure or causal identification is insufficient. They remain in production unchanged pending future governance; this recommendation creates no replacement.
- `GRP-INF-005` keeps five cheap-triage hypotheses outside the formal candidate set for missing exact component evidence.
- `GRP-INF-006` keeps four plausible A&E routes research-needed. `HYP-INF-LAYER-AE-005` is separately blocked by the existing repetition contribution and needs no duplicate vote.

## Existing Relationship proposals

Sixteen **newly undecided** retype-review-only proposals require individual decisions: `REL-INF-004`, `005`, `010`, `011`, `012`, `013`, `017`, `018`, `019`, `020`, `024`, `027`, `030`, `031`, `033`, and `034`. They mostly distinguish independent causality from calculation, composition, semantic overlap or timing. `APPROVE_RETYPE_REVIEW_ONLY` authorizes review disposition only; it does not retype production. Twelve other revision candidates already point to prior pilot/Psychological human decisions and require no duplicate vote. All existing production propositions remain unchanged.

## New Relationship candidate

`REL-CAND-INF-LAYER-0001` (`INF-061 → PSY-016`) receives `KEEP_RESEARCH_NEEDED`. Its bounded proposed meaning is that an encountered statement of one behavior's prevalence in a named referent group may shift the recipient's later perceived prevalence toward the stated value. The field study's **plans versus actual visits** mismatch and bundled comparative feedback in the other trial prevent governance-ready exact alignment. Do not claim a universal positive direction, actual Social norm change, injunctive norm, or behavior. The three sourceFindings and `EVA-REL-CAND-INF-LAYER-0001` remain candidate-only. The related normative-feedback A&E hypothesis shares the possible contribution and cannot be added independently.

## HappeningType identities

`HT-CAND-INF-LAYER-0001` receives `GOVERN_INACTIVE_IDENTITY` as an individual recommendation. It is a single displayed reference-group prevalence statement, distinct from the already governed multi-panel comparative feedback identity `HT-V1-PSY-LAYER-005`. Identity governance would not establish efficacy, truthfulness, source credibility, legality, feasibility, practitioner use or activation. Existing pilot/PSY identities are reused by reference, not reminted.

## EffectAssertions

No new formal EffectAssertion is recommended. Four serious A&E hypotheses remain research-needed; a fifth repetition route remains blocked by `CONTRIB-PSY-LAYER-REPETITION-001`. Five prior INF-F03 candidate effects remain historical research-needed records. There is no direct RDS, RelationalState or generic-context target.

## EvidenceAssessment dependencies

The sole new candidate assessment is `EVA-REL-CAND-INF-LAYER-0001`: `INSUFFICIENT`, low confidence for the exact general proposition. A source on screening intention reports a null added-feedback benefit for **intention**, not a null for perceived norm. Review papers, included studies and shared datasets are not independent replications. This candidate assessment is not recommended for materialization while the assertion remains research-needed.

## Construct/ontology questions

Preserve availability/exposure/attention, objective/perceived message properties, source property/source perception, actual/perceived norms, content/appraisal, volume/load, guidance/action and veracity/belief distinctions. `HYP-INF-F03-H20` remains blocked: `INF-013` conceptual complexity and `INF-077` surface complexity cannot be collapsed or repaired by selecting one measure. No alias, definition, class or target change is recommended here.

## Architecture blockers

`HYP-INF-F03-H20` is an acknowledged prior blocker. `ARCH-INF-LAYER-0001` asks for future governance of versioned RDS derivations and constituent/aggregate causal-source use. `META-INF-LAYER-0001` keeps blocked `RDS-0001` metadata unresolved. The latter two are blocked decision units, not implementation authority. `ASTRA-INF-LAYER-001` preserves the H20 specialist-review question without stalling this Layer.

## Future source registrations

No canonical registration occurs now. If a human later approves `HT-CAND-INF-LAYER-0001`, the future source manifest includes **two** candidate sources (`SRC-CAND-INF-LAYER-013/015`) for identity provenance, subject to exact DOI/PMID and version verification. `SRC-CAND-INF-LAYER-014` supports only a research-needed bundled effect route. `SRC-551` is already canonical. Eleven INF-F03 pilot queue rows stay candidate-only, including two with unresolved authoritative identity. The registry preserves access depth and source-overlap caveats.

## Proposed future materialization set

| Class | Exact future recommendation |
|---|---:|
| Relationships | 0 |
| HappeningTypes | 1 (`HT-CAND-INF-LAYER-0001`, inactive only) |
| EffectAssertions | 0 |
| EvidenceAssessments | 0 |

No future materialization occurs without an exact human decision and subsequent source verification.

## Explicit exclusions

No candidate source is registered, no existing Relationship is retyped/revised/deprecated, no Driver or RDS definition changes, no INF-F03 blocked metadata is repaired, no Network State binding is created, and no Psychological repetition contribution is duplicated. The prior pilot and Psychological reviews remain authoritative. No new Family or Layer is started.

## Activation boundary

**NO ACTIVATION recommendation is authorized by this review.** Identity approval, scientific effect approval, activation, practitioner actionability and quantitative execution remain separate decisions. The Layer PR must remain open and unmerged until human scientific governance.
