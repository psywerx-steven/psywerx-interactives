# Actions & Events V1 authorized execution

Authority: current user instruction, authorized human governor; 2026-09-06.
Architecture and bounded infrastructure only. No new scientific population or
activation, second real Family audit, B01–B05 resolution, ontology changes,
numeric model/ranking, live ingestion or deployment-setting changes.

## Milestones and acceptance gates

1. Reconcile current repository and govern AE01–AE12 on PR #15. Update draft
   semantics, run experimental/baseline validation and CI, independently review,
   commit and merge only if clean.
2. From merged main create `implementation/actions-events-v1`. Add modular
   contracts, evidence normalization, lossless same-identity read bridge,
   eligibility predicates, candidate workspace and safe Family runner.
3. Complete eight-Layer synthetic fixtures, adversarial tests, read-only whole
   ontology coverage, production documentation/prompts and CI. Commit, review,
   open and merge implementation PR only after all gates pass.
4. On merged main repeat synthetic/inventory checks. Create scale-up readiness,
   compare PSY-F03/INF-F03/SOC-F07 mechanically, recommend but do not audit Family
   #2, update morning brief and save final validated handoff.

Validation: existing five Python suites (96 tests), scenario suite (21 tests /
811 entities), draft and production meta/schema checks, new synthetic/bridge/
runner tests, source/evidence/lifecycle/RDS checks, deterministic regeneration
in an isolated checkout, raw-byte scientific protection, Python/JS parsing,
local links, diff scope and Linux/Windows CI. Tests are never weakened to pass.

## Current checkpoint

- Repository/remote confirmed: psywerx-steven/psywerx-interactives.
- Clean proposal branch at `3331273fcfd72121fe831904c282b87c372eaf55`.
- Local main `2636dd9a8b7bad1da3d7dfa21db5fe877f4de4e1`; remote/PR verification pending.
- Current milestone: 1, baseline/contract review and governance materialization.
- Completed: local identity, branch, clean worktree and known baseline check.
- Tests this assignment: none yet.
- Blockers: none known; new governance questions will be recorded explicitly
  as BLOCKED_NEEDS_GOVERNANCE_INPUT without halting independent permitted work.
- Exact restart: read governed documents and complete proposal/contracts, verify
  remote PR/main, record exact AE approvals, then validate PR #15.
- Automatic pre-existing Pages build after authorized merge is acknowledged;
  never manually invoke or change deployment logic.

## Milestone 1: architecture materialization validated locally

- Remote main and PR #15 exactly match supplied SHAs; PR open/draft/mergeable.
- No applicable AGENTS.md found in repository or parent chain. Reviewed governed
  D01–D14, lifecycle, Family workflow, migration/BIO activation records, proposal,
  schema and validator structure. Launch-time unpopulated text is historical.
- Added `docs/governance/ACTIONS_EVENTS_V1_GOVERNANCE_DECISION.md` recording the
  twelve exact outcomes; AE05/AE10 are MODIFY/APPROVE-AS-MODIFIED. No science approved.
- Draft property vocabulary now uses clarified descriptors with schema-level
  property constraints; dry-run action gates now include feasibility/legal/ethics.
- 51 experimental tests + 96 baseline Python + 21 scenario tests passed;
  schema/meta/reference, deterministic regeneration (isolated copy), links,
  compilation/parsing and protected originals passed. Original 234 files unchanged.
- Protected-original check allows exactly the newly authorized governance record,
  not changes to any protected pre-existing file. This is scope accounting, not
  a relaxation of scientific integrity.
- Exact next step: commit/push governance milestone, inspect final-head Linux/
  Windows CI and complete PR diff, mark PR ready and merge using exact head gate.
